import React from "react";
import "./SelectedProject.scss"

interface Props{
    desc?: string;
    projName?: string;
}

export const SelectedProject: React.FC<Props> = ({desc, projName}) => {
    return(<div>
        
    </div>)
}